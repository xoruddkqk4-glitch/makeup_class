# 🏫 보강 알리미 (온라인 교무실 보강 현황 웹 앱)

구글 사이트(Google Sites) 기반 온라인 교무실에 임베드하여 사용하는 **학교 보강 관리 및 현황 공유 웹 애플리케이션**입니다.  
구글 사이트 임베드 환경에 최적화하여 **상단 세로 공간 및 모달창 높이를 대폭 축소한 콤팩트 레이아웃**, **보강 교사 전달 확인 체크박스**, **0초 즉시 반응 낙관적 UI 성능 최적화**가 적용되어 있습니다.

---

## 📌 주요 기능 (Key Features)

- ↔️ **테이블 열 순서 개편**:
  - `날짜(요일)` ➔ `교시` ➔ `교실` ➔ `보강 교사(알림)` ➔ **`결강 교사(원교사)`** ➔ **`보강 사유`** ➔ `작업` 순서로 위치 변경
- ✅ **보강 교사 전달 확인 체크박스 (뱃지: `✅ 본인확인` / `🔔 미확인`)**:
  - **셀 내 체크박스 배치**: 보강 교사 셀 카드 내부에 **체크박스** 및 **`🔔 미확인` / `✅ 본인확인` 뱃지** 배치
  - **원클릭 확인 토글**: 보강 교사가 해당 명단을 확인하고 체크박스를 클릭하면 카드 전체가 에메랄드 톤(`✅ 본인확인`)으로 즉시 변경
  - **DB 상태 자동 저장**: 체크 여부가 구글 스프레드시트 9번째 열(`확인여부`)에 비동기로 자동 저장
- 📋 **모달 폼 필수/선택 입력 항목 구분**:
  - **1~4번 필수 입력**: `1. 날짜`, `2. 교시`, `3. 교실`, `4. 보강 교사` (미입력 시 제출 불가)
  - **5~6번 선택 입력**: `5. 보강 유발 사유 (선택)`, `6. 결강 교사 (선택)` (입력하지 않고 바로 저장 가능)
- 📐 **테이블 툴바 날짜 표기 1줄 고정 최적화**:
  - 기간 선택 시 날짜 정보(`[ 2026-08-31(월) ~ 2026-09-06(일) ]`) 텍스트 크기 축소(`0.775rem`) 및 `white-space: nowrap` 고정
- ⚡ **데이터 등록/수정/삭제/체크 반응 속도 획기적 향상 (성능 최적화)**:
  - **낙관적 UI (Optimistic UI)** 적용: 사용자 버튼 클릭 및 체크박스 토글 즉시 0초 만에 화면 변경
- 🚪 **'학급' ➔ '교실' 체계 전면 개편 (드롭다운 순서: 4층 ➔ 3층 ➔ 2층 ➔ 1층 ➔ 지하)**:
  - **교실 범위**: `4층(401~416)`, `3층(301~316)`, `2층(201~216)`, `1층(101~115)`, `지하(B01~B14)`
- 🔔 **보강 교사 열 특별 시각화 강조 (핵심 기능)**:
  - **헤더 및 셀 강조**: 에메랄드 그린 배경 + 뱃지 카드(`sub-teacher-card`)에 **체크박스 및 [본인확인/미확인] 배지** 적용
- 📅 **기간 빠른 선택 (오늘 / 이번 주 / 이번 달 / 전체 보기)**:
  - 접속 시 자동으로 **오늘** 날짜의 보강 목록 표시
- 🏷️ **사용자 관리형 보강 사유 퀵 태그 (조퇴/외출 분리)**:
  - `#조퇴`와 `#외출` 분리, `[+ 태그 추가]` 지원
- 🔍 **드롭다운 기반 교실 & 사유 열 필터**:
  - **`🔄 필터 초기화` 버튼**: 모든 열 필터 원클릭 초기화
- 🔓 **전 교사 작성 및 수정 가능**: 누구나 자유롭게 등록, 수정, 삭제, 전달 확인 가능
- 📊 **구글 스프레드시트 DB 자동 연동**: 데이터베이스(`보강알리미_DB`) 자동 연동 및 이력 영구 저장 (`ID | 날짜 | 교시 | 교실 | 원교사 | 보강교사 | 사유 | 등록시각 | 확인여부`)

---

## 📂 프로젝트 구조 (Project Structure)

```text
홍익-보강 알리미/
├── Code.gs       # Google Apps Script 백엔드 (1~4번 필수/5~6번 선택 백엔드 검증, toggleSubstituteConfirm API)
├── index.html    # 프론트엔드 (열 순서 변경, 본인확인 뱃지, 모달 5~6번 선택 항목 처리)
└── README.md     # 프로젝트 설명 및 배포 가이드 문서
```

---

## 📋 최근 수정 사항 (Latest Updates)

- **테이블 열 순서 개편**: 결강 교사(원교사) ➔ 보강 사유 순서로 열 위치 변경
- **뱃지 텍스트 수정**: `확인완료` ➔ **`본인확인`** 텍스트 변경
- **모달 폼 필수/선택 항목 구분**: 1~4번은 필수 입력, 5~6번은 선택 입력으로 변경

---

## 🌐 구글 사이트(Google Sites) 배포 가이드

1. **Google Apps Script 프로젝트 생성**:
   - [`Code.gs`](file:///c:/Users/user/Desktop/appsscript/%ED%99%8D%EC%9D%B5-%EB%B3%B4%EA%B0%95%20%EC%95%8C%EB%A6%AC%EB%AF%B8/Code.gs)와 [`index.html`](file:///c:/Users/user/Desktop/appsscript/%ED%99%8D%EC%9D%B5-%EB%B3%B4%EA%B0%95%20%EC%95%8C%EB%A6%AC%EB%AF%B8/index.html) 코드를 복사하여 저장합니다.
2. **웹 앱 배포**:
   - 우측 상단 **[배포] ➔ [새 배포] ➔ [웹 앱]** 선택
   - **실행 대상**: `나 (Me)`
   - **액세스할 수 있는 사용자**: `모든 사용자 (Anyone)`
   - 배포 후 생성된 **웹 앱 URL** 복사
3. **구글 사이트 임베드**:
   - 구글 사이트 편집기에서 **[삽입] ➔ [URL로 삽입]** 선택 후 복사한 URL을 붙여넣고 게시합니다.
