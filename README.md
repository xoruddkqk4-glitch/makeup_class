# 🏫 보강 알리미 (온라인 교무실 보강 현황 웹 앱)

구글 사이트(Google Sites) 기반 온라인 교무실에 임베드하여 사용하는 **학교 보강 관리 및 현황 공유 웹 애플리케이션**입니다.  
구글 사이트 임베드 환경에 최적화하여 **상단 세로 공간 및 모달창 높이를 대폭 축소한 콤팩트 레이아웃**으로 디자인되었습니다.

---

## 📌 주요 기능 (Key Features)

- ↔️ **구글 사이트 전용 2열 가로 확장 모달창 (높이 대폭 축소)**:
  - 기존 1열 세로 긴 폼 ➔ **2열 가로 확장 폼 그리드(`max-width: 800px`)**로 재배치
  - 좌측(날짜, 교시, 학급) & 우측(보강 교사, 보강 사유, 결강 교사) 2열 구성으로 모달 세로 높이를 절반으로 감소시켜 구글 사이트 임베드 시 스크롤 없이 편하게 입력 가능
- 📐 **구글 사이트 임베드 전용 초슬림 콤팩트 레이아웃**:
  - 부가 설명 텍스트 및 중복 마진 제거 ➔ **'보강 알리미' 메인 제목만 슬림하게 배치**
  - 조작 패널을 테이블 헤더 툴바 내부로 완전 통합하여 세로 여백 최소화
- 🔔 **보강 교사 열 특별 시각화 강조 (핵심 기능)**:
  - **헤더 강조**: 진한 에메랄드 딥 그린 그라데이션 + 🔔 `보강 교사 (알림)` 노란 종 아이콘 헤더
  - **셀 및 카드 강조**: 은은한 민트 톤 배경(`td.td-highlight-sub`) + 하얀 둥근 뱃지 카드(`sub-teacher-card`)에 **큰 볼드 텍스트 및 [보강] 배지** 적용
  - **필터 강조**: 🔔 `보강교사 검색...` 필터 입력란도 에메랄드 포인트 처리
- 📅 **기간 빠른 선택 (오늘 / 이번 주 / 이번 달 / 전체 보기)**:
  - 접속 시 자동으로 **오늘** 날짜의 보강 목록 표시
  - **`이번 주`**, **`이번 달`**, **`전체 보기`** 퀵 버튼으로 간편한 기간 조회
- 🏷️ **사용자 관리형 보강 사유 퀵 태그 (조퇴/외출 분리)**:
  - `#조퇴`와 `#외출` 분리
  - `[+ 태그 추가]` 버튼으로 커스텀 사유 태그 생성 및 삭제 가능 (`localStorage` 자동 저장)
- 🔍 **드롭다운 기반 학급 & 사유 열 필터**:
  - **학급 필터**: 전체 학급, 1/2/3학년 전체, 1학년 1반~3학년 9반 개별 선택 드롭다운
  - **사유 필터**: 사유 태그 목록 기반 드롭다운 선택
  - **`🔄 필터 초기화` 버튼**: 모든 열 필터 원클릭 초기화
- ✏️ **아이콘 전용 깔끔한 수정 및 삭제 버튼**:
  - 텍스트 줄바꿈 없는 깔끔한 아이콘 형태(`title` 툴팁 지원)의 수정 및 삭제 버튼 제공
- ➕ **모달 기반 보강 등록/수정** (요청된 컬럼 & 입력 순서 준수):
  1. **날짜 지정**: 달력 피커 UI (`YYYY-MM-DD (요일)` 형식 고정 표시)
  2. **교시 선택**: 1교시 ~ 7교시 원클릭 칩 버튼
  3. **학급 선택**: 1~3학년 x 1~9반 선택
  4. **보강 교사**: 텍스트 직접 입력
  5. **보강 유발 사유**: 텍스트 직접 입력 및 사용자 커스텀 태그 선택
  6. **결강 교사(원교사)**: 텍스트 직접 입력
- 📊 **테이블 컬럼 순서**: `날짜(요일)` ➔ `교시` ➔ `학급` ➔ **`보강 교사(강조)`** ➔ `보강 사유` ➔ `결강 교사(원교사)` ➔ `작업(수정/삭제)`
- 🔓 **전 교사 작성 및 수정 가능**: 권한 제약 없이 누구나 자유롭게 등록, 수정, 삭제 가능
- 📊 **구글 스프레드시트 DB 자동 연동**: 데이터베이스(`보강알리미_DB`) 자동 연동 및 이력 영구 저장

---

## 📂 프로젝트 구조 (Project Structure)

```text
홍익-보강 알리미/
├── Code.gs       # Google Apps Script 백엔드 (스프레드시트 DB 연동 & CRUD API)
├── index.html    # 프론트엔드 (2열 가로 확장 모달, 슬림 콤팩트 레이아웃, 모달)
└── README.md     # 프로젝트 설명 및 배포 가이드 문서
```

---

## 📋 최근 수정 사항 (Latest Updates)

- **보강 등록/수정 모달창 2열 가로 확장 레이아웃 적용**:
  - 모달 가로폭 800px 확대 및 2열 그리드 배치로 세로 높이 절반 감소 ➔ 구글 사이트 임베드 시 입력 용이성 극대화
- **구글 사이트 최적화 콤팩트 레이아웃 적용**: 상단 툴바 통합 및 세로 공간 최소화

---

## 🌐 구글 사이트(Google Sites) 배포 가이드

1. **Google Apps Script 프로젝트 생성**:
   - [`Code.gs`](file:///c:/Users/user/Desktop/appsscript/%ED%99%8D%EC%9D%B5-%EB%B3%B4%EA%B0%95%20%EC%95%8C%EB%A6%AC%EB%AF%B8/Code.gs)와 [`index.html`](file:///c:/Users/user/Desktop/appsscript/%ED%99%8D%EC%9D%B5-%EB%B3%B4%EA%B0%95%20%EC%95%8C%EB%A6%AC%EB%AF%B8/index.html) 코드를 복사하여 저장합니다.
2. **웹 앱 배포**:
   - 우측 상단 **[배포] ➔ [새 배포] ➔ [웹 앱]** 선택
   - **실행 대상**: `나 (Me)`
   - **액세스할 수 있는 사용자**: `모든 사용자 (Anyone)`
   - 배포 후 생성된 **웹 앱 URL** 복사
3. **구글 사이트 임베드**:
   - 구글 사이트 편집기에서 **[삽입] ➔ [URL로 삽입]** 선택 후 복사한 URL을 붙여넣고 게시합니다.
